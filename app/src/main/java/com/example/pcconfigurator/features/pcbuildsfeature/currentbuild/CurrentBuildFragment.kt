package com.example.pcconfigurator.features.pcbuildsfeature.currentbuild

import androidx.fragment.app.Fragment
import by.kirich1409.viewbindingdelegate.viewBinding
import com.example.pcconfigurator.R
import com.example.pcconfigurator.databinding.FragmentCurrentBuildBinding

class CurrentBuildFragment : Fragment(R.layout.fragment_current_build) {
    private val binding: FragmentCurrentBuildBinding by viewBinding(FragmentCurrentBuildBinding::bind)
}