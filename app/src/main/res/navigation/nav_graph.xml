<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/builds">

    <fragment
        android:id="@+id/builds"
        android:name="com.example.pcconfigurator.features.pcBuilds.BuildsContainerFragment"
        android:label="@string/builds"
        tools:layout="@layout/fragment_builds_container" />

    <fragment
        android:id="@+id/categories"
        android:name="com.example.pcconfigurator.features.categories.CategoriesContainerFragment"
        android:label="@string/categories"
        tools:layout="@layout/fragment_categories_container" />

    <fragment
        android:id="@+id/favorites"
        android:name="com.example.pcconfigurator.features.favorites.FavoritesContainerFragment"
        android:label="@string/favorites"
        tools:layout="@layout/fragment_favorites_container" />

    <fragment
        android:id="@+id/profile"
        android:name="com.example.pcconfigurator.features.profile.ProfileContainerFragment"
        android:label="@string/profile"
        tools:layout="@layout/fragment_profile_container" />

</navigation>